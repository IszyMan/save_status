name: status_saver
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.2
  video_player: ^2.10.0
  path_provider: ^2.1.5
  flutter_launcher_icons: ^0.14.4
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/statusly_icon.png

flutter_launcher_icons:
  android: true
  image_path: "assets/images/statusly_icon.png"
